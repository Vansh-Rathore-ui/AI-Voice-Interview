{
  "name": "ai-interviewer",
  "version": "1.0.0",
  "description": "AI-powered interview system with voice support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd frontend && npm install",
    "setup": "npm run install:all && npm run build"
  },
  "dependencies": {
    "child-process-promise": "^2.2.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "uuid": "^9.0.1",
    "ws": "^8.17.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
